# Copy this file to .env and fill in the values.
# Docker Compose automatically loads .env from the same directory as the compose file.

# OpenWeatherMap API key for the weather widget on the overview dashboard.
# Get a free key at https://openweathermap.org/api — the "Current Weather Data" free tier is sufficient.
OPENWEATHERMAP_API_KEY=
