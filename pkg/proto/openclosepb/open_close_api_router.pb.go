// Code generated by protoc-gen-router. DO NOT EDIT.
//lint:file-ignore SA1019 Routers intentionally use a deprecated packaged - they are deprecated themselves

package openclosepb

import (
	context "context"
	fmt "fmt"
	router "github.com/smart-core-os/sc-bos/pkg/router"
	grpc "google.golang.org/grpc"
	io "io"
)

// ApiRouter is a OpenCloseApiServer that allows routing named requests to specific OpenCloseApiClient
// Deprecated: routing is now handled dynamically by [node.Node].
type ApiRouter struct {
	UnimplementedOpenCloseApiServer

	router.Router
}

// compile time check that we implement the interface we need
var _ OpenCloseApiServer = (*ApiRouter)(nil)

// NewApiRouter constructs a new empty ApiRouter with the provided options.
// Deprecated: routing is now handled dynamically by [node.Node].
func NewApiRouter(opts ...router.Option) *ApiRouter {
	return &ApiRouter{
		Router: router.NewRouter(opts...),
	}
}

// WithOpenCloseApiClientFactory instructs the router to create a new
// client the first time Get is called for that name.
func WithOpenCloseApiClientFactory(f func(name string) (OpenCloseApiClient, error)) router.Option {
	return router.WithFactory(func(name string) (any, error) {
		return f(name)
	})
}

func (r *ApiRouter) Register(server grpc.ServiceRegistrar) {
	RegisterOpenCloseApiServer(server, r)
}

// Add extends Router.Add to panic if client is not of type OpenCloseApiClient.
func (r *ApiRouter) Add(name string, client any) any {
	if !r.HoldsType(client) {
		panic(fmt.Sprintf("not correct type: client of type %T is not a OpenCloseApiClient", client))
	}
	return r.Router.Add(name, client)
}

func (r *ApiRouter) HoldsType(client any) bool {
	_, ok := client.(OpenCloseApiClient)
	return ok
}

func (r *ApiRouter) AddOpenCloseApiClient(name string, client OpenCloseApiClient) OpenCloseApiClient {
	res := r.Add(name, client)
	if res == nil {
		return nil
	}
	return res.(OpenCloseApiClient)
}

func (r *ApiRouter) RemoveOpenCloseApiClient(name string) OpenCloseApiClient {
	res := r.Remove(name)
	if res == nil {
		return nil
	}
	return res.(OpenCloseApiClient)
}

func (r *ApiRouter) GetOpenCloseApiClient(name string) (OpenCloseApiClient, error) {
	res, err := r.Get(name)
	if err != nil {
		return nil, err
	}
	if res == nil {
		return nil, nil
	}
	return res.(OpenCloseApiClient), nil
}

func (r *ApiRouter) GetPositions(ctx context.Context, request *GetOpenClosePositionsRequest) (*OpenClosePositions, error) {
	child, err := r.GetOpenCloseApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.GetPositions(ctx, request)
}

func (r *ApiRouter) UpdatePositions(ctx context.Context, request *UpdateOpenClosePositionsRequest) (*OpenClosePositions, error) {
	child, err := r.GetOpenCloseApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.UpdatePositions(ctx, request)
}

func (r *ApiRouter) Stop(ctx context.Context, request *StopOpenCloseRequest) (*OpenClosePositions, error) {
	child, err := r.GetOpenCloseApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.Stop(ctx, request)
}

func (r *ApiRouter) PullPositions(request *PullOpenClosePositionsRequest, server OpenCloseApi_PullPositionsServer) error {
	child, err := r.GetOpenCloseApiClient(request.Name)
	if err != nil {
		return err
	}

	// so we can cancel our forwarding request if we can't send responses to our caller
	reqCtx, reqDone := context.WithCancel(server.Context())
	// issue the request
	stream, err := child.PullPositions(reqCtx, request)
	if err != nil {
		return err
	}

	// send the stream header
	header, err := stream.Header()
	if err != nil {
		return err
	}
	if err = server.SendHeader(header); err != nil {
		return err
	}

	// send all the messages
	// false means the error is from the child, true means the error is from the caller
	var callerError bool
	for {
		// Impl note: we could improve throughput here by issuing the Recv and Send in different goroutines, but we're doing
		// it synchronously until we have a need to change the behaviour

		var msg *PullOpenClosePositionsResponse
		msg, err = stream.Recv()
		if err != nil {
			break
		}

		err = server.Send(msg)
		if err != nil {
			callerError = true
			break
		}
	}

	// err is guaranteed to be non-nil as it's the only way to exit the loop
	if callerError {
		// cancel the request
		reqDone()
		return err
	} else {
		if trailer := stream.Trailer(); trailer != nil {
			server.SetTrailer(trailer)
		}
		if err == io.EOF {
			return nil
		}
		return err
	}
}
