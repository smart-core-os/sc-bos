// Code generated by protoc-gen-router. DO NOT EDIT.

package meterpb

import (
	context "context"
	fmt "fmt"
	router "github.com/smart-core-os/sc-golang/pkg/router"
	grpc "google.golang.org/grpc"
)

// InfoRouter is a MeterInfoServer that allows routing named requests to specific MeterInfoClient
type InfoRouter struct {
	UnimplementedMeterInfoServer

	router.Router
}

// compile time check that we implement the interface we need
var _ MeterInfoServer = (*InfoRouter)(nil)

func NewInfoRouter(opts ...router.Option) *InfoRouter {
	return &InfoRouter{
		Router: router.NewRouter(opts...),
	}
}

// WithMeterInfoClientFactory instructs the router to create a new
// client the first time Get is called for that name.
func WithMeterInfoClientFactory(f func(name string) (MeterInfoClient, error)) router.Option {
	return router.WithFactory(func(name string) (any, error) {
		return f(name)
	})
}

func (r *InfoRouter) Register(server grpc.ServiceRegistrar) {
	RegisterMeterInfoServer(server, r)
}

// Add extends Router.Add to panic if client is not of type MeterInfoClient.
func (r *InfoRouter) Add(name string, client any) any {
	if !r.HoldsType(client) {
		panic(fmt.Sprintf("not correct type: client of type %T is not a MeterInfoClient", client))
	}
	return r.Router.Add(name, client)
}

func (r *InfoRouter) HoldsType(client any) bool {
	_, ok := client.(MeterInfoClient)
	return ok
}

func (r *InfoRouter) AddMeterInfoClient(name string, client MeterInfoClient) MeterInfoClient {
	res := r.Add(name, client)
	if res == nil {
		return nil
	}
	return res.(MeterInfoClient)
}

func (r *InfoRouter) RemoveMeterInfoClient(name string) MeterInfoClient {
	res := r.Remove(name)
	if res == nil {
		return nil
	}
	return res.(MeterInfoClient)
}

func (r *InfoRouter) GetMeterInfoClient(name string) (MeterInfoClient, error) {
	res, err := r.Get(name)
	if err != nil {
		return nil, err
	}
	if res == nil {
		return nil, nil
	}
	return res.(MeterInfoClient), nil
}

func (r *InfoRouter) DescribeMeterReading(ctx context.Context, request *DescribeMeterReadingRequest) (*MeterReadingSupport, error) {
	child, err := r.GetMeterInfoClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.DescribeMeterReading(ctx, request)
}
