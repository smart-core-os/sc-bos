// Code generated by protoc-gen-router. DO NOT EDIT.

package logpb

import (
	context "context"
	fmt "fmt"
	router "github.com/smart-core-os/sc-bos/sc-golang/pkg/router"
	grpc "google.golang.org/grpc"
	io "io"
)

// ApiRouter is a LogApiServer that allows routing named requests to specific LogApiClient
type ApiRouter struct {
	UnimplementedLogApiServer

	router.Router
}

// compile time check that we implement the interface we need
var _ LogApiServer = (*ApiRouter)(nil)

func NewApiRouter(opts ...router.Option) *ApiRouter {
	return &ApiRouter{
		Router: router.NewRouter(opts...),
	}
}

// WithLogApiClientFactory instructs the router to create a new
// client the first time Get is called for that name.
func WithLogApiClientFactory(f func(name string) (LogApiClient, error)) router.Option {
	return router.WithFactory(func(name string) (any, error) {
		return f(name)
	})
}

func (r *ApiRouter) Register(server grpc.ServiceRegistrar) {
	RegisterLogApiServer(server, r)
}

// Add extends Router.Add to panic if client is not of type LogApiClient.
func (r *ApiRouter) Add(name string, client any) any {
	if !r.HoldsType(client) {
		panic(fmt.Sprintf("not correct type: client of type %T is not a LogApiClient", client))
	}
	return r.Router.Add(name, client)
}

func (r *ApiRouter) HoldsType(client any) bool {
	_, ok := client.(LogApiClient)
	return ok
}

func (r *ApiRouter) AddLogApiClient(name string, client LogApiClient) LogApiClient {
	res := r.Add(name, client)
	if res == nil {
		return nil
	}
	return res.(LogApiClient)
}

func (r *ApiRouter) RemoveLogApiClient(name string) LogApiClient {
	res := r.Remove(name)
	if res == nil {
		return nil
	}
	return res.(LogApiClient)
}

func (r *ApiRouter) GetLogApiClient(name string) (LogApiClient, error) {
	res, err := r.Get(name)
	if err != nil {
		return nil, err
	}
	if res == nil {
		return nil, nil
	}
	return res.(LogApiClient), nil
}

func (r *ApiRouter) PullLogMessages(request *PullLogMessagesRequest, server LogApi_PullLogMessagesServer) error {
	child, err := r.GetLogApiClient(request.Name)
	if err != nil {
		return err
	}

	// so we can cancel our forwarding request if we can't send responses to our caller
	reqCtx, reqDone := context.WithCancel(server.Context())
	// issue the request
	stream, err := child.PullLogMessages(reqCtx, request)
	if err != nil {
		return err
	}

	// send the stream header
	header, err := stream.Header()
	if err != nil {
		return err
	}
	if err = server.SendHeader(header); err != nil {
		return err
	}

	// send all the messages
	// false means the error is from the child, true means the error is from the caller
	var callerError bool
	for {
		// Impl note: we could improve throughput here by issuing the Recv and Send in different goroutines, but we're doing
		// it synchronously until we have a need to change the behaviour

		var msg *PullLogMessagesResponse
		msg, err = stream.Recv()
		if err != nil {
			break
		}

		err = server.Send(msg)
		if err != nil {
			callerError = true
			break
		}
	}

	// err is guaranteed to be non-nil as it's the only way to exit the loop
	if callerError {
		// cancel the request
		reqDone()
		return err
	} else {
		if trailer := stream.Trailer(); trailer != nil {
			server.SetTrailer(trailer)
		}
		if err == io.EOF {
			return nil
		}
		return err
	}
}

func (r *ApiRouter) GetLogLevel(ctx context.Context, request *GetLogLevelRequest) (*LogLevel, error) {
	child, err := r.GetLogApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.GetLogLevel(ctx, request)
}

func (r *ApiRouter) PullLogLevel(request *PullLogLevelRequest, server LogApi_PullLogLevelServer) error {
	child, err := r.GetLogApiClient(request.Name)
	if err != nil {
		return err
	}

	// so we can cancel our forwarding request if we can't send responses to our caller
	reqCtx, reqDone := context.WithCancel(server.Context())
	// issue the request
	stream, err := child.PullLogLevel(reqCtx, request)
	if err != nil {
		return err
	}

	// send the stream header
	header, err := stream.Header()
	if err != nil {
		return err
	}
	if err = server.SendHeader(header); err != nil {
		return err
	}

	// send all the messages
	// false means the error is from the child, true means the error is from the caller
	var callerError bool
	for {
		// Impl note: we could improve throughput here by issuing the Recv and Send in different goroutines, but we're doing
		// it synchronously until we have a need to change the behaviour

		var msg *PullLogLevelResponse
		msg, err = stream.Recv()
		if err != nil {
			break
		}

		err = server.Send(msg)
		if err != nil {
			callerError = true
			break
		}
	}

	// err is guaranteed to be non-nil as it's the only way to exit the loop
	if callerError {
		// cancel the request
		reqDone()
		return err
	} else {
		if trailer := stream.Trailer(); trailer != nil {
			server.SetTrailer(trailer)
		}
		if err == io.EOF {
			return nil
		}
		return err
	}
}

func (r *ApiRouter) UpdateLogLevel(ctx context.Context, request *UpdateLogLevelRequest) (*LogLevel, error) {
	child, err := r.GetLogApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.UpdateLogLevel(ctx, request)
}

func (r *ApiRouter) GetLogMetadata(ctx context.Context, request *GetLogMetadataRequest) (*LogMetadata, error) {
	child, err := r.GetLogApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.GetLogMetadata(ctx, request)
}

func (r *ApiRouter) PullLogMetadata(request *PullLogMetadataRequest, server LogApi_PullLogMetadataServer) error {
	child, err := r.GetLogApiClient(request.Name)
	if err != nil {
		return err
	}

	// so we can cancel our forwarding request if we can't send responses to our caller
	reqCtx, reqDone := context.WithCancel(server.Context())
	// issue the request
	stream, err := child.PullLogMetadata(reqCtx, request)
	if err != nil {
		return err
	}

	// send the stream header
	header, err := stream.Header()
	if err != nil {
		return err
	}
	if err = server.SendHeader(header); err != nil {
		return err
	}

	// send all the messages
	// false means the error is from the child, true means the error is from the caller
	var callerError bool
	for {
		// Impl note: we could improve throughput here by issuing the Recv and Send in different goroutines, but we're doing
		// it synchronously until we have a need to change the behaviour

		var msg *PullLogMetadataResponse
		msg, err = stream.Recv()
		if err != nil {
			break
		}

		err = server.Send(msg)
		if err != nil {
			callerError = true
			break
		}
	}

	// err is guaranteed to be non-nil as it's the only way to exit the loop
	if callerError {
		// cancel the request
		reqDone()
		return err
	} else {
		if trailer := stream.Trailer(); trailer != nil {
			server.SetTrailer(trailer)
		}
		if err == io.EOF {
			return nil
		}
		return err
	}
}

func (r *ApiRouter) GetDownloadLogUrl(ctx context.Context, request *GetDownloadLogUrlRequest) (*GetDownloadLogUrlResponse, error) {
	child, err := r.GetLogApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.GetDownloadLogUrl(ctx, request)
}
