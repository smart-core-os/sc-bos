// Code generated by protoc-gen-router. DO NOT EDIT.

package storagepb

import (
	context "context"
	fmt "fmt"
	router "github.com/smart-core-os/sc-golang/pkg/router"
	grpc "google.golang.org/grpc"
	io "io"
)

// ApiRouter is a StorageApiServer that allows routing named requests to specific StorageApiClient
type ApiRouter struct {
	UnimplementedStorageApiServer

	router.Router
}

// compile time check that we implement the interface we need
var _ StorageApiServer = (*ApiRouter)(nil)

func NewApiRouter(opts ...router.Option) *ApiRouter {
	return &ApiRouter{
		Router: router.NewRouter(opts...),
	}
}

// WithStorageApiClientFactory instructs the router to create a new
// client the first time Get is called for that name.
func WithStorageApiClientFactory(f func(name string) (StorageApiClient, error)) router.Option {
	return router.WithFactory(func(name string) (any, error) {
		return f(name)
	})
}

func (r *ApiRouter) Register(server grpc.ServiceRegistrar) {
	RegisterStorageApiServer(server, r)
}

// Add extends Router.Add to panic if client is not of type StorageApiClient.
func (r *ApiRouter) Add(name string, client any) any {
	if !r.HoldsType(client) {
		panic(fmt.Sprintf("not correct type: client of type %T is not a StorageApiClient", client))
	}
	return r.Router.Add(name, client)
}

func (r *ApiRouter) HoldsType(client any) bool {
	_, ok := client.(StorageApiClient)
	return ok
}

func (r *ApiRouter) AddStorageApiClient(name string, client StorageApiClient) StorageApiClient {
	res := r.Add(name, client)
	if res == nil {
		return nil
	}
	return res.(StorageApiClient)
}

func (r *ApiRouter) RemoveStorageApiClient(name string) StorageApiClient {
	res := r.Remove(name)
	if res == nil {
		return nil
	}
	return res.(StorageApiClient)
}

func (r *ApiRouter) GetStorageApiClient(name string) (StorageApiClient, error) {
	res, err := r.Get(name)
	if err != nil {
		return nil, err
	}
	if res == nil {
		return nil, nil
	}
	return res.(StorageApiClient), nil
}

func (r *ApiRouter) GetStorage(ctx context.Context, request *GetStorageRequest) (*Storage, error) {
	child, err := r.GetStorageApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.GetStorage(ctx, request)
}

func (r *ApiRouter) PullStorage(request *PullStorageRequest, server StorageApi_PullStorageServer) error {
	child, err := r.GetStorageApiClient(request.Name)
	if err != nil {
		return err
	}

	// so we can cancel our forwarding request if we can't send responses to our caller
	reqCtx, reqDone := context.WithCancel(server.Context())
	// issue the request
	stream, err := child.PullStorage(reqCtx, request)
	if err != nil {
		return err
	}

	// send the stream header
	header, err := stream.Header()
	if err != nil {
		return err
	}
	if err = server.SendHeader(header); err != nil {
		return err
	}

	// send all the messages
	// false means the error is from the child, true means the error is from the caller
	var callerError bool
	for {
		// Impl note: we could improve throughput here by issuing the Recv and Send in different goroutines, but we're doing
		// it synchronously until we have a need to change the behaviour

		var msg *PullStorageResponse
		msg, err = stream.Recv()
		if err != nil {
			break
		}

		err = server.Send(msg)
		if err != nil {
			callerError = true
			break
		}
	}

	// err is guaranteed to be non-nil as it's the only way to exit the loop
	if callerError {
		// cancel the request
		reqDone()
		return err
	} else {
		if trailer := stream.Trailer(); trailer != nil {
			server.SetTrailer(trailer)
		}
		if err == io.EOF {
			return nil
		}
		return err
	}
}
