// Code generated by protoc-gen-router. DO NOT EDIT.
//lint:file-ignore SA1019 Routers intentionally use a deprecated packaged - they are deprecated themselves

package lockunlockpb

import (
	context "context"
	fmt "fmt"
	router "github.com/smart-core-os/sc-bos/pkg/router"
	grpc "google.golang.org/grpc"
	io "io"
)

// ApiRouter is a LockUnlockApiServer that allows routing named requests to specific LockUnlockApiClient
// Deprecated: routing is now handled dynamically by [node.Node].
type ApiRouter struct {
	UnimplementedLockUnlockApiServer

	router.Router
}

// compile time check that we implement the interface we need
var _ LockUnlockApiServer = (*ApiRouter)(nil)

// NewApiRouter constructs a new empty ApiRouter with the provided options.
// Deprecated: routing is now handled dynamically by [node.Node].
func NewApiRouter(opts ...router.Option) *ApiRouter {
	return &ApiRouter{
		Router: router.NewRouter(opts...),
	}
}

// WithLockUnlockApiClientFactory instructs the router to create a new
// client the first time Get is called for that name.
func WithLockUnlockApiClientFactory(f func(name string) (LockUnlockApiClient, error)) router.Option {
	return router.WithFactory(func(name string) (any, error) {
		return f(name)
	})
}

func (r *ApiRouter) Register(server grpc.ServiceRegistrar) {
	RegisterLockUnlockApiServer(server, r)
}

// Add extends Router.Add to panic if client is not of type LockUnlockApiClient.
func (r *ApiRouter) Add(name string, client any) any {
	if !r.HoldsType(client) {
		panic(fmt.Sprintf("not correct type: client of type %T is not a LockUnlockApiClient", client))
	}
	return r.Router.Add(name, client)
}

func (r *ApiRouter) HoldsType(client any) bool {
	_, ok := client.(LockUnlockApiClient)
	return ok
}

func (r *ApiRouter) AddLockUnlockApiClient(name string, client LockUnlockApiClient) LockUnlockApiClient {
	res := r.Add(name, client)
	if res == nil {
		return nil
	}
	return res.(LockUnlockApiClient)
}

func (r *ApiRouter) RemoveLockUnlockApiClient(name string) LockUnlockApiClient {
	res := r.Remove(name)
	if res == nil {
		return nil
	}
	return res.(LockUnlockApiClient)
}

func (r *ApiRouter) GetLockUnlockApiClient(name string) (LockUnlockApiClient, error) {
	res, err := r.Get(name)
	if err != nil {
		return nil, err
	}
	if res == nil {
		return nil, nil
	}
	return res.(LockUnlockApiClient), nil
}

func (r *ApiRouter) GetLockUnlock(ctx context.Context, request *GetLockUnlockRequest) (*LockUnlock, error) {
	child, err := r.GetLockUnlockApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.GetLockUnlock(ctx, request)
}

func (r *ApiRouter) UpdateLockUnlock(ctx context.Context, request *UpdateLockUnlockRequest) (*LockUnlock, error) {
	child, err := r.GetLockUnlockApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.UpdateLockUnlock(ctx, request)
}

func (r *ApiRouter) PullLockUnlock(request *PullLockUnlockRequest, server LockUnlockApi_PullLockUnlockServer) error {
	child, err := r.GetLockUnlockApiClient(request.Name)
	if err != nil {
		return err
	}

	// so we can cancel our forwarding request if we can't send responses to our caller
	reqCtx, reqDone := context.WithCancel(server.Context())
	// issue the request
	stream, err := child.PullLockUnlock(reqCtx, request)
	if err != nil {
		return err
	}

	// send the stream header
	header, err := stream.Header()
	if err != nil {
		return err
	}
	if err = server.SendHeader(header); err != nil {
		return err
	}

	// send all the messages
	// false means the error is from the child, true means the error is from the caller
	var callerError bool
	for {
		// Impl note: we could improve throughput here by issuing the Recv and Send in different goroutines, but we're doing
		// it synchronously until we have a need to change the behaviour

		var msg *PullLockUnlockResponse
		msg, err = stream.Recv()
		if err != nil {
			break
		}

		err = server.Send(msg)
		if err != nil {
			callerError = true
			break
		}
	}

	// err is guaranteed to be non-nil as it's the only way to exit the loop
	if callerError {
		// cancel the request
		reqDone()
		return err
	} else {
		if trailer := stream.Trailer(); trailer != nil {
			server.SetTrailer(trailer)
		}
		if err == io.EOF {
			return nil
		}
		return err
	}
}
