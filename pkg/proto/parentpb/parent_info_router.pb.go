// Code generated by protoc-gen-router. DO NOT EDIT.

package parentpb

import (
	fmt "fmt"
	router "github.com/smart-core-os/sc-bos/pkg/router"
	grpc "google.golang.org/grpc"
)

// InfoRouter is a ParentInfoServer that allows routing named requests to specific ParentInfoClient
// Deprecated: routing is now handled dynamically by [node.Node].
type InfoRouter struct {
	UnimplementedParentInfoServer

	router.Router
}

// compile time check that we implement the interface we need
var _ ParentInfoServer = (*InfoRouter)(nil)

// NewInfoRouter constructs a new empty InfoRouter with the provided options.
// Deprecated: routing is now handled dynamically by [node.Node].
func NewInfoRouter(opts ...router.Option) *InfoRouter {
	return &InfoRouter{
		Router: router.NewRouter(opts...),
	}
}

// WithParentInfoClientFactory instructs the router to create a new
// client the first time Get is called for that name.
func WithParentInfoClientFactory(f func(name string) (ParentInfoClient, error)) router.Option {
	return router.WithFactory(func(name string) (any, error) {
		return f(name)
	})
}

func (r *InfoRouter) Register(server grpc.ServiceRegistrar) {
	RegisterParentInfoServer(server, r)
}

// Add extends Router.Add to panic if client is not of type ParentInfoClient.
func (r *InfoRouter) Add(name string, client any) any {
	if !r.HoldsType(client) {
		panic(fmt.Sprintf("not correct type: client of type %T is not a ParentInfoClient", client))
	}
	return r.Router.Add(name, client)
}

func (r *InfoRouter) HoldsType(client any) bool {
	_, ok := client.(ParentInfoClient)
	return ok
}

func (r *InfoRouter) AddParentInfoClient(name string, client ParentInfoClient) ParentInfoClient {
	res := r.Add(name, client)
	if res == nil {
		return nil
	}
	return res.(ParentInfoClient)
}

func (r *InfoRouter) RemoveParentInfoClient(name string) ParentInfoClient {
	res := r.Remove(name)
	if res == nil {
		return nil
	}
	return res.(ParentInfoClient)
}

func (r *InfoRouter) GetParentInfoClient(name string) (ParentInfoClient, error) {
	res, err := r.Get(name)
	if err != nil {
		return nil, err
	}
	if res == nil {
		return nil, nil
	}
	return res.(ParentInfoClient), nil
}
