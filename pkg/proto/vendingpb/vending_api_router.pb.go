// Code generated by protoc-gen-router. DO NOT EDIT.

package vendingpb

import (
	context "context"
	fmt "fmt"
	router "github.com/smart-core-os/sc-bos/pkg/router"
	grpc "google.golang.org/grpc"
	io "io"
)

// ApiRouter is a VendingApiServer that allows routing named requests to specific VendingApiClient
// Deprecated: routing is now handled dynamically by [node.Node].
type ApiRouter struct {
	UnimplementedVendingApiServer

	router.Router
}

// compile time check that we implement the interface we need
var _ VendingApiServer = (*ApiRouter)(nil)

// NewApiRouter constructs a new empty ApiRouter with the provided options.
// Deprecated: routing is now handled dynamically by [node.Node].
func NewApiRouter(opts ...router.Option) *ApiRouter {
	return &ApiRouter{
		Router: router.NewRouter(opts...),
	}
}

// WithVendingApiClientFactory instructs the router to create a new
// client the first time Get is called for that name.
func WithVendingApiClientFactory(f func(name string) (VendingApiClient, error)) router.Option {
	return router.WithFactory(func(name string) (any, error) {
		return f(name)
	})
}

func (r *ApiRouter) Register(server grpc.ServiceRegistrar) {
	RegisterVendingApiServer(server, r)
}

// Add extends Router.Add to panic if client is not of type VendingApiClient.
func (r *ApiRouter) Add(name string, client any) any {
	if !r.HoldsType(client) {
		panic(fmt.Sprintf("not correct type: client of type %T is not a VendingApiClient", client))
	}
	return r.Router.Add(name, client)
}

func (r *ApiRouter) HoldsType(client any) bool {
	_, ok := client.(VendingApiClient)
	return ok
}

func (r *ApiRouter) AddVendingApiClient(name string, client VendingApiClient) VendingApiClient {
	res := r.Add(name, client)
	if res == nil {
		return nil
	}
	return res.(VendingApiClient)
}

func (r *ApiRouter) RemoveVendingApiClient(name string) VendingApiClient {
	res := r.Remove(name)
	if res == nil {
		return nil
	}
	return res.(VendingApiClient)
}

func (r *ApiRouter) GetVendingApiClient(name string) (VendingApiClient, error) {
	res, err := r.Get(name)
	if err != nil {
		return nil, err
	}
	if res == nil {
		return nil, nil
	}
	return res.(VendingApiClient), nil
}

func (r *ApiRouter) ListConsumables(ctx context.Context, request *ListConsumablesRequest) (*ListConsumablesResponse, error) {
	child, err := r.GetVendingApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.ListConsumables(ctx, request)
}

func (r *ApiRouter) PullConsumables(request *PullConsumablesRequest, server VendingApi_PullConsumablesServer) error {
	child, err := r.GetVendingApiClient(request.Name)
	if err != nil {
		return err
	}

	// so we can cancel our forwarding request if we can't send responses to our caller
	reqCtx, reqDone := context.WithCancel(server.Context())
	// issue the request
	stream, err := child.PullConsumables(reqCtx, request)
	if err != nil {
		return err
	}

	// send the stream header
	header, err := stream.Header()
	if err != nil {
		return err
	}
	if err = server.SendHeader(header); err != nil {
		return err
	}

	// send all the messages
	// false means the error is from the child, true means the error is from the caller
	var callerError bool
	for {
		// Impl note: we could improve throughput here by issuing the Recv and Send in different goroutines, but we're doing
		// it synchronously until we have a need to change the behaviour

		var msg *PullConsumablesResponse
		msg, err = stream.Recv()
		if err != nil {
			break
		}

		err = server.Send(msg)
		if err != nil {
			callerError = true
			break
		}
	}

	// err is guaranteed to be non-nil as it's the only way to exit the loop
	if callerError {
		// cancel the request
		reqDone()
		return err
	} else {
		if trailer := stream.Trailer(); trailer != nil {
			server.SetTrailer(trailer)
		}
		if err == io.EOF {
			return nil
		}
		return err
	}
}

func (r *ApiRouter) GetStock(ctx context.Context, request *GetStockRequest) (*Consumable_Stock, error) {
	child, err := r.GetVendingApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.GetStock(ctx, request)
}

func (r *ApiRouter) UpdateStock(ctx context.Context, request *UpdateStockRequest) (*Consumable_Stock, error) {
	child, err := r.GetVendingApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.UpdateStock(ctx, request)
}

func (r *ApiRouter) PullStock(request *PullStockRequest, server VendingApi_PullStockServer) error {
	child, err := r.GetVendingApiClient(request.Name)
	if err != nil {
		return err
	}

	// so we can cancel our forwarding request if we can't send responses to our caller
	reqCtx, reqDone := context.WithCancel(server.Context())
	// issue the request
	stream, err := child.PullStock(reqCtx, request)
	if err != nil {
		return err
	}

	// send the stream header
	header, err := stream.Header()
	if err != nil {
		return err
	}
	if err = server.SendHeader(header); err != nil {
		return err
	}

	// send all the messages
	// false means the error is from the child, true means the error is from the caller
	var callerError bool
	for {
		// Impl note: we could improve throughput here by issuing the Recv and Send in different goroutines, but we're doing
		// it synchronously until we have a need to change the behaviour

		var msg *PullStockResponse
		msg, err = stream.Recv()
		if err != nil {
			break
		}

		err = server.Send(msg)
		if err != nil {
			callerError = true
			break
		}
	}

	// err is guaranteed to be non-nil as it's the only way to exit the loop
	if callerError {
		// cancel the request
		reqDone()
		return err
	} else {
		if trailer := stream.Trailer(); trailer != nil {
			server.SetTrailer(trailer)
		}
		if err == io.EOF {
			return nil
		}
		return err
	}
}

func (r *ApiRouter) ListInventory(ctx context.Context, request *ListInventoryRequest) (*ListInventoryResponse, error) {
	child, err := r.GetVendingApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.ListInventory(ctx, request)
}

func (r *ApiRouter) PullInventory(request *PullInventoryRequest, server VendingApi_PullInventoryServer) error {
	child, err := r.GetVendingApiClient(request.Name)
	if err != nil {
		return err
	}

	// so we can cancel our forwarding request if we can't send responses to our caller
	reqCtx, reqDone := context.WithCancel(server.Context())
	// issue the request
	stream, err := child.PullInventory(reqCtx, request)
	if err != nil {
		return err
	}

	// send the stream header
	header, err := stream.Header()
	if err != nil {
		return err
	}
	if err = server.SendHeader(header); err != nil {
		return err
	}

	// send all the messages
	// false means the error is from the child, true means the error is from the caller
	var callerError bool
	for {
		// Impl note: we could improve throughput here by issuing the Recv and Send in different goroutines, but we're doing
		// it synchronously until we have a need to change the behaviour

		var msg *PullInventoryResponse
		msg, err = stream.Recv()
		if err != nil {
			break
		}

		err = server.Send(msg)
		if err != nil {
			callerError = true
			break
		}
	}

	// err is guaranteed to be non-nil as it's the only way to exit the loop
	if callerError {
		// cancel the request
		reqDone()
		return err
	} else {
		if trailer := stream.Trailer(); trailer != nil {
			server.SetTrailer(trailer)
		}
		if err == io.EOF {
			return nil
		}
		return err
	}
}

func (r *ApiRouter) Dispense(ctx context.Context, request *DispenseRequest) (*Consumable_Stock, error) {
	child, err := r.GetVendingApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.Dispense(ctx, request)
}

func (r *ApiRouter) StopDispense(ctx context.Context, request *StopDispenseRequest) (*Consumable_Stock, error) {
	child, err := r.GetVendingApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.StopDispense(ctx, request)
}
