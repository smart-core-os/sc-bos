// Code generated by protoc-gen-router. DO NOT EDIT.
//lint:file-ignore SA1019 Routers intentionally use a deprecated packaged - they are deprecated themselves

package speakerpb

import (
	context "context"
	fmt "fmt"
	typespb "github.com/smart-core-os/sc-bos/pkg/proto/typespb"
	router "github.com/smart-core-os/sc-bos/pkg/router"
	grpc "google.golang.org/grpc"
	io "io"
)

// ApiRouter is a SpeakerApiServer that allows routing named requests to specific SpeakerApiClient
// Deprecated: routing is now handled dynamically by [node.Node].
type ApiRouter struct {
	UnimplementedSpeakerApiServer

	router.Router
}

// compile time check that we implement the interface we need
var _ SpeakerApiServer = (*ApiRouter)(nil)

// NewApiRouter constructs a new empty ApiRouter with the provided options.
// Deprecated: routing is now handled dynamically by [node.Node].
func NewApiRouter(opts ...router.Option) *ApiRouter {
	return &ApiRouter{
		Router: router.NewRouter(opts...),
	}
}

// WithSpeakerApiClientFactory instructs the router to create a new
// client the first time Get is called for that name.
func WithSpeakerApiClientFactory(f func(name string) (SpeakerApiClient, error)) router.Option {
	return router.WithFactory(func(name string) (any, error) {
		return f(name)
	})
}

func (r *ApiRouter) Register(server grpc.ServiceRegistrar) {
	RegisterSpeakerApiServer(server, r)
}

// Add extends Router.Add to panic if client is not of type SpeakerApiClient.
func (r *ApiRouter) Add(name string, client any) any {
	if !r.HoldsType(client) {
		panic(fmt.Sprintf("not correct type: client of type %T is not a SpeakerApiClient", client))
	}
	return r.Router.Add(name, client)
}

func (r *ApiRouter) HoldsType(client any) bool {
	_, ok := client.(SpeakerApiClient)
	return ok
}

func (r *ApiRouter) AddSpeakerApiClient(name string, client SpeakerApiClient) SpeakerApiClient {
	res := r.Add(name, client)
	if res == nil {
		return nil
	}
	return res.(SpeakerApiClient)
}

func (r *ApiRouter) RemoveSpeakerApiClient(name string) SpeakerApiClient {
	res := r.Remove(name)
	if res == nil {
		return nil
	}
	return res.(SpeakerApiClient)
}

func (r *ApiRouter) GetSpeakerApiClient(name string) (SpeakerApiClient, error) {
	res, err := r.Get(name)
	if err != nil {
		return nil, err
	}
	if res == nil {
		return nil, nil
	}
	return res.(SpeakerApiClient), nil
}

func (r *ApiRouter) GetVolume(ctx context.Context, request *GetSpeakerVolumeRequest) (*typespb.AudioLevel, error) {
	child, err := r.GetSpeakerApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.GetVolume(ctx, request)
}

func (r *ApiRouter) UpdateVolume(ctx context.Context, request *UpdateSpeakerVolumeRequest) (*typespb.AudioLevel, error) {
	child, err := r.GetSpeakerApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.UpdateVolume(ctx, request)
}

func (r *ApiRouter) PullVolume(request *PullSpeakerVolumeRequest, server SpeakerApi_PullVolumeServer) error {
	child, err := r.GetSpeakerApiClient(request.Name)
	if err != nil {
		return err
	}

	// so we can cancel our forwarding request if we can't send responses to our caller
	reqCtx, reqDone := context.WithCancel(server.Context())
	// issue the request
	stream, err := child.PullVolume(reqCtx, request)
	if err != nil {
		return err
	}

	// send the stream header
	header, err := stream.Header()
	if err != nil {
		return err
	}
	if err = server.SendHeader(header); err != nil {
		return err
	}

	// send all the messages
	// false means the error is from the child, true means the error is from the caller
	var callerError bool
	for {
		// Impl note: we could improve throughput here by issuing the Recv and Send in different goroutines, but we're doing
		// it synchronously until we have a need to change the behaviour

		var msg *PullSpeakerVolumeResponse
		msg, err = stream.Recv()
		if err != nil {
			break
		}

		err = server.Send(msg)
		if err != nil {
			callerError = true
			break
		}
	}

	// err is guaranteed to be non-nil as it's the only way to exit the loop
	if callerError {
		// cancel the request
		reqDone()
		return err
	} else {
		if trailer := stream.Trailer(); trailer != nil {
			server.SetTrailer(trailer)
		}
		if err == io.EOF {
			return nil
		}
		return err
	}
}
