// Code generated by protoc-gen-router. DO NOT EDIT.

package gen

import (
	context "context"
	fmt "fmt"
	router "github.com/smart-core-os/sc-golang/pkg/router"
	grpc "google.golang.org/grpc"
	io "io"
)

// AllocationApiRouter is a AllocationApiServer that allows routing named requests to specific AllocationApiClient
type AllocationApiRouter struct {
	UnimplementedAllocationApiServer

	router.Router
}

// compile time check that we implement the interface we need
var _ AllocationApiServer = (*AllocationApiRouter)(nil)

func NewAllocationApiRouter(opts ...router.Option) *AllocationApiRouter {
	return &AllocationApiRouter{
		Router: router.NewRouter(opts...),
	}
}

// WithAllocationApiClientFactory instructs the router to create a new
// client the first time Get is called for that name.
func WithAllocationApiClientFactory(f func(name string) (AllocationApiClient, error)) router.Option {
	return router.WithFactory(func(name string) (any, error) {
		return f(name)
	})
}

func (r *AllocationApiRouter) Register(server grpc.ServiceRegistrar) {
	RegisterAllocationApiServer(server, r)
}

// Add extends Router.Add to panic if client is not of type AllocationApiClient.
func (r *AllocationApiRouter) Add(name string, client any) any {
	if !r.HoldsType(client) {
		panic(fmt.Sprintf("not correct type: client of type %T is not a AllocationApiClient", client))
	}
	return r.Router.Add(name, client)
}

func (r *AllocationApiRouter) HoldsType(client any) bool {
	_, ok := client.(AllocationApiClient)
	return ok
}

func (r *AllocationApiRouter) AddAllocationApiClient(name string, client AllocationApiClient) AllocationApiClient {
	res := r.Add(name, client)
	if res == nil {
		return nil
	}
	return res.(AllocationApiClient)
}

func (r *AllocationApiRouter) RemoveAllocationApiClient(name string) AllocationApiClient {
	res := r.Remove(name)
	if res == nil {
		return nil
	}
	return res.(AllocationApiClient)
}

func (r *AllocationApiRouter) GetAllocationApiClient(name string) (AllocationApiClient, error) {
	res, err := r.Get(name)
	if err != nil {
		return nil, err
	}
	if res == nil {
		return nil, nil
	}
	return res.(AllocationApiClient), nil
}

func (r *AllocationApiRouter) GetAllocation(ctx context.Context, request *GetAllocationRequest) (*Allocation, error) {
	child, err := r.GetAllocationApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.GetAllocation(ctx, request)
}

func (r *AllocationApiRouter) UpdateAllocation(ctx context.Context, request *UpdateAllocationRequest) (*Allocation, error) {
	child, err := r.GetAllocationApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.UpdateAllocation(ctx, request)
}

func (r *AllocationApiRouter) PullAllocation(request *PullAllocationRequest, server AllocationApi_PullAllocationServer) error {
	child, err := r.GetAllocationApiClient(request.Name)
	if err != nil {
		return err
	}

	// so we can cancel our forwarding request if we can't send responses to our caller
	reqCtx, reqDone := context.WithCancel(server.Context())
	// issue the request
	stream, err := child.PullAllocation(reqCtx, request)
	if err != nil {
		return err
	}

	// send the stream header
	header, err := stream.Header()
	if err != nil {
		return err
	}
	if err = server.SendHeader(header); err != nil {
		return err
	}

	// send all the messages
	// false means the error is from the child, true means the error is from the caller
	var callerError bool
	for {
		// Impl note: we could improve throughput here by issuing the Recv and Send in different goroutines, but we're doing
		// it synchronously until we have a need to change the behaviour

		var msg *PullAllocationResponse
		msg, err = stream.Recv()
		if err != nil {
			break
		}

		err = server.Send(msg)
		if err != nil {
			callerError = true
			break
		}
	}

	// err is guaranteed to be non-nil as it's the only way to exit the loop
	if callerError {
		// cancel the request
		reqDone()
		return err
	} else {
		if trailer := stream.Trailer(); trailer != nil {
			server.SetTrailer(trailer)
		}
		if err == io.EOF {
			return nil
		}
		return err
	}
}
