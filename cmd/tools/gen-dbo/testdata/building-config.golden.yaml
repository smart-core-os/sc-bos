# Generated by cmd/tools/gen-dbo — Google Digital Buildings building configuration.
#
# PLACEHOLDERS TO RESOLVE before use:
#   * type: a water meter gets the canonical METERS/WM_STANDARD. An energy-only
#     electricity meter has no canonical DBO type (every METERS/EM_PWM* requires
#     power_sensor), so it gets the METERS/EM_INITIAL passthrough — reclassify it (or
#     add power reporting) once its capabilities are known.
#   * cloud_device_id: BOS has no Google device-registry id, so a deterministic
#     numeric placeholder is derived from the device name. Replace each with the real
#     numeric Google device-registry id before ingest.
#
# Point names are DBO standard fields, so each translation present_value path is an
# identity mapping. units.values maps the DBO unit name to the raw device string.
0ab74275-8e53-5fc8-8091-8af8fc0504c7:
  cloud_device_id: "16124151007419001386"
  code: van/uk/brum/ugs/meters/solar-pv
  translation:
    energy_accumulator:
      present_value: points.energy_accumulator.present_value
      units:
        key: pointset.points.energy_accumulator.units
        values:
          kilowatt_hours: kWh
    exported_energy_accumulator:
      present_value: points.exported_energy_accumulator.present_value
      units:
        key: pointset.points.exported_energy_accumulator.units
        values:
          kilowatt_hours: kWh
  type: METERS/EM_INITIAL
8fac2ffd-c21e-5147-ac87-e7f70b30d94e:
  code: UK-BRUM-UGS1
  type: FACILITIES/BUILDING
3292df9b-c08b-5b96-bf84-102548c8be2e:
  cloud_device_id: "14453892414031090193"
  code: van/uk/brum/ugs/meters/elec-main
  translation:
    energy_accumulator:
      present_value: points.energy_accumulator.present_value
      units:
        key: pointset.points.energy_accumulator.units
        values:
          kilowatt_hours: kWh
  type: METERS/EM_INITIAL
ae5a925e-0c09-554c-9820-5acf36969e88:
  cloud_device_id: "16737413062013815971"
  code: van/uk/brum/ugs/meters/water-main
  translation:
    water_volume_accumulator:
      present_value: points.water_volume_accumulator.present_value
      units:
        key: pointset.points.water_volume_accumulator.units
        values:
          cubic_meters: m³
  type: METERS/WM_STANDARD
