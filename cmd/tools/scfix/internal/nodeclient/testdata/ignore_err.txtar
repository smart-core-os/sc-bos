expected_changes: 1

Test ignoring the error response from n.Client.

-- input/test.go --
package test

import (
	"github.com/smart-core-os/sc-api/go/traits"
	"github.com/smart-core-os/sc-bos/pkg/node"
)

func example(n *node.Node) {
	var client traits.OnOffApiClient
	n.Client(&client)
}
-- output/test.go --
package test

import (
	"github.com/smart-core-os/sc-api/go/traits"
	"github.com/smart-core-os/sc-bos/pkg/node"
)

func example(n *node.Node) {
	client := traits.NewOnOffApiClient(n.ClientConn())
}
